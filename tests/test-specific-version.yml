name: Test specific git-cliff version
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Create a minimal cliff.toml config
        run: |
          cat > cliff.toml << 'EOF'
          [changelog]
          header = "# Changelog\n"
          body = """
          {% if version %}\
              ## [{{ version | replace(from="v", to="") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
          {% else %}\
              ## [unreleased]
          {% endif %}\
          {% for group, commits in commits | group_by(attribute="group") %}
              ### {{ group | upper_first }}
              {% for commit in commits %}
                  - {{ commit.message | upper_first }}\
              {% endfor %}
          {% endfor %}\n
          """
          trim = true
          footer = "<!-- generated by git-cliff -->\n"

          [git]
          conventional_commits = true
          EOF

      - id: action
        uses: .
        with:
          version: v2.11.0
          config: cliff.toml
          args: -v --unreleased --output git-cliff/CHANGELOG.md

      - name: Assert content output is set
        env:
          ACTUAL: ${{ steps.action.outputs.content }}
        run: |
          echo "actual content: $ACTUAL"
          if [ -z "$ACTUAL" ]; then
            echo "FAIL: expected content output to be non-empty"
            exit 1
          fi
          echo "PASS: content output is set"

      - name: Assert changelog file exists and is readable
        env:
          CHANGELOG: ${{ steps.action.outputs.changelog }}
        run: |
          echo "checking file: $CHANGELOG"
          if [ ! -f "$CHANGELOG" ]; then
            echo "FAIL: changelog file does not exist at $CHANGELOG"
            exit 1
          fi
          CONTENT=$(cat "$CHANGELOG")
          echo "file content: $CONTENT"
          if [ -z "$CONTENT" ]; then
            echo "FAIL: changelog file is empty"
            exit 1
          fi
          echo "PASS: changelog file exists and has content"
